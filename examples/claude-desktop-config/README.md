# Claude Desktop configuration example

Copy `claude_desktop_config.example.json` into your Claude Desktop configuration file:

- macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`

Restart Claude Desktop. Claude will discover `get_available_ui_blocks` and `render_ui_block`.
